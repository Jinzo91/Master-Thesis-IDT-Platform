apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ddb-claim
  namespace: idt
spec:
  # storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi