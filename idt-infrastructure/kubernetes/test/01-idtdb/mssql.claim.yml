apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-claim-test
  namespace: idt-test
spec:
  # storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi