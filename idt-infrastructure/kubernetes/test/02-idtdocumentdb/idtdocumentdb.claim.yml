apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ddb-claim-test
  namespace: idt-test
spec:
  # storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi