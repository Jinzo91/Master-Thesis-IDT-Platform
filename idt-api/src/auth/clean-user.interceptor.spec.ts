import { CleanUserInterceptor } from './clean-user.interceptor';

describe('CleanUserInterceptor', () => {
  it('should be defined', () => {
    expect(new CleanUserInterceptor()).toBeDefined();
  });
});
